#cloud-config
hostname: nginxvault
manage_etc_hosts: true
fqdn: nginxvault.home.lab
ssh_pwauth: true

users:
  - name: liam
    lock_passwd: false # Makes sure the user is not locked 
    groups: sudo
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys:
      - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIGDqH9jyXQKNjML31DEETH/lAo0nO0n+WHB+/yuPvcCb

package_upgrade: true
packages:
  - qemu-guest-agent
  - nginx

write_files:
  - path: /etc/nginx/sites-available/secrets
    permissons: '0644'
    content: |
      server {
          listen 80;
          server_name _;

          root /opt/secrets;
          autoindex off;

          location / {
              allow 192.168.99.0/24;
              allow 192.168.22.0/24;
              deny all;
          }
      }

  - path: /tmp/manifest
    permissons: '0750'
    content: |
      romm-mariadb-password
      romm-auth-secret-key
      romm-screenscapper-user
      romm-screenscrapper-password
      romm-mariadb-root-password

  - path: /tmp/50unattended-upgrades
    defer: true
    owner: root:root
    permissions: '0644'
    content: |
        Unattended-Upgrade::Allowed-Origins {
        // Base Ubuntu release packages - as shipped on install day, rarely changes
        "${distro_id}:${distro_codename}";

        // Security patches only - CVEs and critical fixes, low risk, fast turnaround
        "${distro_id}:${distro_codename}-security";

        // Stable bug fixes and broader updates - tested before release, still low risk
        // Remove this line if you want security-only updates
        "${distro_id}:${distro_codename}-updates";

        };
        // NOTE: Only packages from Ubuntu's official repos are updated with this config.
        // Third-party repos (Docker, Hashicorp, PPAs etc) are never touched - update manually
        // or via a separate weekly apt upgrade cron job.
        // noble-backports and noble-proposed are intentionally excluded - too risky for auto-update

        // Sends logs to syslog /var/log/syslog
        Unattended-Upgrade::SyslogEnable "true";
        Unattended-Upgrade::SyslogFacility "daemon";

        // Auto reboot 3am if users are logged in or not
        Unattended-Upgrade::Automatic-Reboot "true";
        Unattended-Upgrade::Automatic-Reboot-Time "03:00";

        // Cleanup
        Unattended-Upgrade::Remove-Unused-Dependencies "true";
        Unattended-Upgrade::Remove-Unused-Kernel-Packages "true";
        Unattended-Upgrade::LogRotate "6";

        Unattended-Upgrade::Package-Blacklist {
        };
        // Normal Unattended Update logs /var/log/unattended-upgrades/

  - path: /tmp/20auto-upgrades
    defer: true
    owner: root:root
    permissions: '0644'
    content: |
        APT::Periodic::Update-Package-Lists "1";
        APT::Periodic::Unattended-Upgrade "1";
        APT::Periodic::AutocleanInterval "7";


runcmd:
  # Set up secrets directory
  - mkdir -p /opt/secrets
  - chown www-data:www-data /opt/secrets
  - chmod 750 /opt/secrets

  # Copy manifest file 
  - cp /tmp/manifest /opt/secrets/manifest
  - chown www-data:www-data /opt/secrets/manifest


  # Enable nginx site
  - rm -f /etc/nginx/sites-enabled/default
  - ln -s /etc/nginx/sites-available/secrets /etc/nginx/sites-enabled/secrets
  - nginx -t
  - systemctl enable nginx
  - systemctl restart nginx

  